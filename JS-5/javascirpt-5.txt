------------------ First -----------------
Object === آبجکت و تعریف آن
1. {}: آکولاد یعنی آبجکت و مقدارش به صورت جفت کلید و مقدار هست
2. Object: با کلمه کلیدی آبجکت هم تعریف میشه
3. برای دسترسی به مقدار باید از کلید استفاده کنیم
4. برعکس آرایه ترتیب مهم نیست 
5. توی آبجکت میشه از بقیه تایپها استفاده کرد اما بصورت کلید :مقدار 
6. از آبجکت هم میشه توی آبجکت استفاده کرد که بهش آبجکت تودر تو میگن یا نستد آبجکت

.
.
.

------------------ Fourth -----------------
Chaining Operator === عملگر زنجیری
با استفاده از این عملگر میشه به لایه های داخلی آبجکت دسترسی داشت تا چند لایه داخل با یه دات یا . نقطه


------------------ Fifth ------------------
Object keys and values === دسترسی به کلید یا مقدار در یک آبجکت
1. Object.keys:یک آرایه از کلیدها میده
2.Object.values: یک آرایه از مقادیر میده


------------------ Sixth ------------------
Spread operator === عملگر اسپرید 
همون کاری که با اسپرید تو آرایه ها انجام میدادیمو انجام میده 


------------------ Seventh ------------------
Object destructuring === دی استراکچر کردن آبجکت 
مثل آرایه کار میکنه منتها اینجا اسمی که برای مقادیر استفاده شده باید استفاده بشه 